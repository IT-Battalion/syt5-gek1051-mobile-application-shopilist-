import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'at.ac.tgm.shopilist',
  appName: 'Shopilist',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
